namespace Shared
{
    public enum InvoiceTypes
    {
        /// <summary>
        /// As described in the case study the only invoice type we have is service
        /// </summary>
        Service = 1
    }
}
